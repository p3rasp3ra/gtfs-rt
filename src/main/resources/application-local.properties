# =============================================================================
# GTFS-RT Application - Local Development Profile
# Usage: ./gradlew bootRun --args='--spring.profiles.active=local'
# =============================================================================

# All services running on localhost (docker compose up infrastructure only)
spring.jpa.show-sql=true

# Debug logging for development
logging.level.com.marszrut=DEBUG
logging.level.org.springframework.kafka=INFO
logging.level.org.hibernate.SQL=DEBUG

